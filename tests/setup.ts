// Global Jest setup, loaded via setupFilesAfterEnv.
//
// React Native Testing Library v14 registers its built-in matchers
// (toBeOnTheScreen, toHaveTextContent, …) automatically and cleans up the
// render tree after each test, so no manual matcher import is required here.
// Add global mocks (e.g. native modules) to this file as features land.
export {};
