// Public theme entry point for TypeScript code. Prefer importing tokens from
// here (`@/theme`) so styling values stay consistent with tailwind.config.js.
export { colors, borderRadius, fontFamily, fontSize } from './tokens';
